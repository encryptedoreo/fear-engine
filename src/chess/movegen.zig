const std = @import("std");

const types = @import("types.zig");

pub fn generateMoves(board: types.Board) types.MoveList {
    _ = board;
    const move_list: types.MoveList = .init();

    return move_list;
}
